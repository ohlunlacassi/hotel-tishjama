import styled from "styled-components";
import { croissantOne } from "../Header";

export default styled.h2`
  text-align: center;
  color: var(--light-brown);
  font-size: 2.3rem;
  font-family: ${croissantOne.style.fontFamily}, sans-serif;
  margin: 1.4rem;
`;
